pub const limit_value: Bool = True
pub const euler_value: String = "res"
pub const tag_value: String = "ab"

fn constructor(constructor: Int, v0: #(Bool, Int), n: String) -> Int {
5
}

pub fn main() {
  let tag_value = {
    5 + 3
  } |> constructor(#(False, 3), {
    let length = euler_value
    let length = [10, 100]
    "abc"
  })
  let prototype = case "bc", 100 |> constructor(#(True, 4), fn(v1, v2) { euler_value }(3, 10.0)) {
    "a" as whole, _ -> euler_value <> "constructor"
    "x" <> _, 1 -> "data" <> "b"
    "res" <> _, 8 as whole -> euler_value <> ""
    v3, _ -> euler_value
  }
  echo case fn(v4, v5) { [42, 42] }(True, 42), tag_value {
    [], 7 -> False
    [b, ..rest], 2 -> False
    [1], 9 -> limit_value
    _, _ -> {
      !limit_value
    } || {
      {
        let z = limit_value
        let acc = 0.1
        z
      }
    }
  }
  echo limit_value
}
